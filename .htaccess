RewriteEngine On

# Force the canonical HTTPS + www host.
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} !^www\.bharatinfrassociate\.com$ [NC]
RewriteRule ^ https://www.bharatinfrassociate.com%{REQUEST_URI} [R=301,L]
